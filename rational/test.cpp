#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include "doctest.h"
#include "rational.h"

TEST_CASE("testing Rational::Normalize() ") {
    // Rational a = Rational(1, 2);
    // bool state = false;
    // if( (a.GetDenominator() == 2) && (a.GetNumerator() == 1)){
    //     state = true;
    // }
    CHECK(1 == 2);
}
