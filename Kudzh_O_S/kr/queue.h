#pragma once

class Queue{

public:
	Queue() = default;
	Queue(int <vector> nums)
	Queue& operation==(const Queue& fst, const Queue& snd)
	Queue& operation+=(Queue& vec, const Queue& num)
	void Push(const Queue& _a)
	void Pop()
	

private:
	int <vector> queue;
	int first;
	int last;
}